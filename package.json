{
  "name": "cv-site-pdf",
  "private": true,
  "version": "1.0.0",
  "description": "Generate assets/cv.pdf from the Jekyll /cv/ page during the build",
  "scripts": {
    "generate:pdf": "node scripts/generate-pdf.js"
  },
  "dependencies": {
    "puppeteer": "^23.0.0"
  }
}
